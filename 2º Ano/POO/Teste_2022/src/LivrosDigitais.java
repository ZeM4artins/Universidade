import java.util.*;

public class LivrosDigitais {

    private Set<Utilizador> users;

    /*
    public Map<String, List<Livro>> livrosPorEditora() {
        Map<String,List<Livro>> aux = new HashMap<>();
        for (Utilizador u: this.users ) {
            for(Livro l : u.getColecao()) {
                aux.getOrDefault(l.getEditora(),new ArrayList<>().add(l));
            }
        }
        return aux;
    }
     */


}
